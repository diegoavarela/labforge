# Required for AI Chat Assistant
LABFORGE_ANTHROPIC_KEY=sk-ant-...

# Required for GitHub OAuth (Push to GitHub feature)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_SECRET=

# Optional: higher GitHub API rate limits for Skills registry
GITHUB_TOKEN=ghp_...

# Database (use this for Docker, change host to localhost for local dev)
DATABASE_URL=postgresql://labforge:labforge@db:5432/labforge

# NextAuth (change to your domain/IP for remote access)
NEXTAUTH_URL=http://localhost:3000
